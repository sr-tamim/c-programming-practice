#include <stdio.h>

int main() {
    int arr[6] = {10, 25, 30, 49, 57, 65}; // declare and initialize array
    int i;

    for (i = 0; i < 6; i++) { // loop through array and print each element
        printf("%d \n", arr[i]);
    }

    return 0;
}
